#ifndef RAYTRACE_MATHUTILS
#define RAYTRACE_MATHUTILS

// add defs for constants and simple macros
#define MAX(a, b) (a > b) ? a : b;
#define MIN(a, b) (a < b) ? a : b;
#define PI 3.1415926535

#endif